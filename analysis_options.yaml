# This file configures the analyzer and linter rules for the project.
# See: https://dart.dev/guides/language/analysis-options

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    # Treat missing required params as errors
    missing_required_param: error
    # Treat missing return as errors
    missing_return: error
  exclude:
    # Exclude generated files from analysis
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - 'lib/generated/**'

linter:
  rules:
    # ---------- Style ----------
    always_declare_return_types: true
    avoid_print: true            # Use a logger instead of print()
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true
    sort_child_properties_last: true
    use_key_in_widget_constructors: true

    # ---------- Correctness ----------
    cancel_subscriptions: true
    close_sinks: true
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true

    # ---------- Null Safety ----------
    cast_nullable_to_non_nullable: true
    null_check_on_nullable_type_parameter: true

    # ---------- Pub ----------
    depend_on_referenced_packages: true
